import Modal from "../Modal";
import Form from "./Form";

const ModalAdsEdit = () => {
  return (
    <Modal title="Editar Anúncio">
      <Form />
    </Modal>
  );
};

export default ModalAdsEdit;
